import React from 'react';
import { View } from 'react-native';
import LoginForm from '../components/LoginForm'; // Preverite pot

const Login = () => {
  return (
    <View style={{ flex: 1 }}>
      <LoginForm />
    </View>
  );
};

export default Login;
